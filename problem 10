#include <iostream>
using namespace std;
int main() {
    int n,max;
    cout << "Enter a number: ";
    cin >> n;
    max = n;
    while (n != 0) {
        if (max < n) {
            max = n;
        }
        cout << "Enter a number: ";
        cin >> n;
    }
    cout << max << endl;
}
