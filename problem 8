#include <iostream>
using namespace std;
int main() {
    int n, a = 0, b;
    cout << "Enter a number: ";
    cin >> n;
    while (n > 0) {
        b = n % 10;
        a = a * 10 + b;
        n = n / 10;
    }
    cout << "Reversed number: " << a << endl;

    return 0;
}
