#include <iostream>
using namespace std;
int main() {
    int a,b, sum = 0;
    cout << "Enter a number: ";
    cin >> a;
    for (int i = 0 ; i <= a ; i++) {
        b=i*i;
        sum = sum + b;
    }
    cout << sum << endl;
}
